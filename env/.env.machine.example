
# Example .env file for machine environment variables
# Copy this file to .env.<env>_machine and fill in the values for your environment.
# Replace <mlflow-server-env-private-ip> and <backend-container-name> with actual values.
#
# ENV_USED: (optional) Label for the environment, e.g., dev, staging, prod
# MLFLOW_TRACKING_PRIVATE_IP_DEV: Private IP of the MLflow server for this environment
# MLFLOW_TRACKING_URI: Full URI to the MLflow tracking server
# EXPERIMENT_NAME, RUN_NAME, DATASET_NAME, ALGO_TYPE, REGISTERED_MODEL_NAME: MLflow experiment/run/model details
# MODEL_SERVER_IP: URL to the backend model server (container or host)
# MODEL_ALIAS: Model alias to use (e.g., champion)

ENV_USED=

# MLflow
MLFLOW_TRACKING_PRIVATE_IP_DEV=
MLFLOW_TRACKING_URI=http://<mlflow-server-env-private-ip>:5050

## MLflow Experiment & Run settings
EXPERIMENT_NAME=fraud_detection_exp_name
RUN_NAME=paysim-dev-machine-v1
DATASET_NAME=paysim-dev.csv
ALGO_TYPE=traditional-ml
REGISTERED_MODEL_NAME=dev.fraud-detection-model

# API
MODEL_SERVER_IP=http://<backend-container-name>:8000
MODEL_ALIAS=champion

