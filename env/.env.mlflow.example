
# Example .env file for MLflow server environment variables
# Copy this file to .env.mlflow-<env> and fill in the values for your environment.
# Replace <env> with your environment (dev, staging, prod, etc).
#
# AWS_ACCESS_KEY_ID / AWS_SECRET_ACCESS_KEY: Credentials for the S3 bucket used by MLflow
# AWS_REGION: AWS region where the S3 bucket is hosted
# MLFLOW_S3_BUCKET: Name of the S3 bucket for MLflow artifacts (e.g., mlops-dev-bkt.store.nus.cloud)
# DB_USER / DB_PASSWORD: Credentials for the PostgreSQL backend store
# REAL_DB_ENDPOINT: Hostname or IP of the PostgreSQL server
# DB_NAME: Name of the MLflow database
# DB_PORT: Port for PostgreSQL (default: 5432)

# AWS for <env>-bkt
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=
MLFLOW_S3_BUCKET=

# PostgreSQL backend store
DB_USER=
DB_PASSWORD=
REAL_DB_ENDPOINT=
DB_NAME=
DB_PORT=