# Staging Environment Configuration
ENV=staging

# AWS Configuration
AWS_ACCESS_KEY_ID=AKIARYSNPSQNQPOS65RT
AWS_SECRET_ACCESS_KEY=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
AWS_REGION=ap-southeast-1

# S3 Configuration
MLFLOW_S3_BUCKET=mlops-bucket.store.nus.cloud
DVC_S3_ENDPOINT_URL=https://mlops-bucket.store.nus.cloud

# Database Configuration (Shared MLflow DB)
DB_USER=ubuntu
DB_PASSWORD=Zojmu3443L
REAL_DB_ENDPOINT=10.0.1.6  # Same database as dev for centralized registry
DB_NAME=mlflow_db  # Same database as dev
DB_PORT=5432

# MLflow Configuration (Staging) - Local access for development
MLFLOW_TRACKING_URI=http://localhost:5051  # For local development/testing
MODEL_SERVER_IP=http://localhost:8081      # For local development/testing
# External access: http://13.212.203.176:5051 and http://13.212.203.176:8081

# MLflow Experiment Settings (Staging)
EXPERIMENT_NAME=fraud_detection_staging
RUN_NAME=paysim-staging
DATASET_NAME=paysim-staging.csv
ALGO_TYPE=traditional-ml
REGISTERED_MODEL_NAME=fraud-detection-model  # Same model, different alias

# API Configuration  
MODEL_ALIAS=staging  # Use staging alias for staging environment
